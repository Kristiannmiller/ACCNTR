// Types
import { SpinnerProps } from '@/components';

const mockSpinnerDefault: SpinnerProps = {};

export const mockSpinner: Record<'default', SpinnerProps> = {
    default: mockSpinnerDefault
};
