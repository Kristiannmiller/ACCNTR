import { Meta, Stories } from '@storybook/blocks';
import * as NavigationMenuStories from './NavigationMenu.stories';

<Meta
    title='Design System/atoms/NavigationMenu/Docs'
    of={NavigationMenuStories}
/>

# NavigationMenu

Description: The NavigationMenu component is a raw component that is largely un-styled. It has a hover state
that opens a content menu when hovered over. The content menu is a list of links that use the Anchor component.

<Stories />
