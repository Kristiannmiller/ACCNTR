import { fileURLToPath } from 'url'
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    globals: true,
    environment: 'jsdom',
    reporters: ['verbose'],
    outputDiffLines: 40,
    outputTruncateLength: 820
  },
  resolve: {
    alias: {
      '@sitecore/byoc': fileURLToPath(new URL('./src', import.meta.url)) + '/index.tsx'
    }
  }
})
